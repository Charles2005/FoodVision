<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/thumbnail"
        android:label="@string/app_name"
        android:roundIcon="@drawable/thumbnail"
        android:supportsRtl="true"
        android:theme="@style/Theme.Food_Vision">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Register"></activity>
        <activity android:name=".activities.AccountPage"></activity>
        <activity android:name=".activities.AdminDashboard"></activity>
        <activity android:name=".activities.AdminLogin"></activity>
        <activity android:name=".activities.Dashboard"></activity>
        <activity android:name=".activities.InformationPage"></activity>
        <activity android:name=".activities.NewDataPage"></activity>
        <activity android:name=".activities.NoDataPage"></activity>
        <activity android:name=".activities.NutritionalInquiry"></activity>
        <activity android:name=".activities.FeedbackPage"></activity>
        <activity android:name=".activities.DataPrivacyPage"></activity>
    </application>

</manifest>